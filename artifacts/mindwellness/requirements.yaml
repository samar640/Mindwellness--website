packages:
  "framer-motion": "For elegant, smooth page transitions and interactive animations that fit the premium spa aesthetic"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility function to efficiently merge Tailwind CSS classes"
  "lucide-react": "Beautiful, consistent icons"

images: []

notes:
  - "The application is fully frontend-driven with no backend API dependencies."
  - "User data (To-Do list) is persisted using browser localStorage."
  - "Audio playback for the breathing exercises relies on the standard HTML5 Audio API. Audio plays only after explicit user interaction (clicking 'Start Session') to comply with modern browser autoplay policies."
